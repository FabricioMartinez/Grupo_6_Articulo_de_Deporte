<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>prueba</h1>
    <ul>
        <% for(let i=0; i<productos.length;i++) {%>
            <li>
                <a href="/prueba/<%= productos[i].id_productos %>"><%= productos[i].name %></a>
                <span>
                    -
                    <% if(productos[i].categoria){ %>
                        <i>
                            <%= productos[i].categoria.opciones  %>
                        </i>
                    <% } %>
                </span>
            </li>
        <% } %>
    </ul>
</body>
</html>